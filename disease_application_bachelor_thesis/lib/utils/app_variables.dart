import 'package:flutter/material.dart';

class AppVariables {
  static final Color lightBlue = Colors.blueGrey;
  static const Shadow textShadow = Shadow(
    offset: Offset(3.0, 3.0),
    blurRadius: 3.0,
    color: Color.fromARGB(100, 0, 0, 0),
  );
}
